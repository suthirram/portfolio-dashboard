# Copy to .env.local and adjust
VITE_API_URL=http://localhost:8080

# Production (Cloudflare Pages): set VITE_API_URL=https://portfolio-dashboard-api.fly.dev
# (no trailing slash, no /api suffix — client.ts appends /api)
